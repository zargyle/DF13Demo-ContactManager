/*
 * @class ${appName}Layout
 * @author ${user} - ${email}
 * @created ${date}
 *
 */

${appName}.${appName}Layout = Backbone.Marionette.Layout.extend({
	template: '${appName}Layout',

	events: {},

	regions: {
		content : '.content'
	},

	onRender: function() {}

});